name: Typos

on:
  pull_request:
  push:
    branches:
      - main
      - master

permissions:
  contents: read

jobs:
  typos:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - uses: crate-ci/typos@master